package com.tuneo.app.ui.theme

import androidx.compose.ui.graphics.Color

// Accueil (Chansons / Vidéos) — fond noir pur
val TuneoBackground = Color(0xFF000000)
val TuneoSurface = Color(0xFF121212)
val TuneoTextPrimary = Color(0xFFFFFFFF)
val TuneoTextSecondary = Color(0xFFB3B3B3)
val TuneoAccentBlue = Color(0xFF2F7BFF) // pastille onglet actif (Chansons/Vidéos)

// Écran lecteur — fond vert très foncé + accent turquoise (fidèle à la capture)
val PlayerBackground = Color(0xFF1C2B23)
val PlayerAccent = Color(0xFF6FE0C6) // titre, contrôles, barre de progression
val PlayerTextSecondary = Color(0xFFAFC2BA)

// Mini-player persistant (bandeau rose/rouge pâle en bas)
val MiniPlayerBackground = Color(0xFFF6D6D2)
val MiniPlayerText = Color(0xFF1A1A1A)
